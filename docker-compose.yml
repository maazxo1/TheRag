version: "3.9"

services:
  # ── Ollama — local LLM inference server ─────────────────────────────────────
  ollama:
    image: ollama/ollama:latest
    container_name: askvault-ollama
    restart: unless-stopped
    volumes:
      - ollama_models:/root/.ollama
    ports:
      - "11434:11434"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ── Model bootstrap — pulls required models on first start ───────────────────
  ollama-setup:
    image: ollama/ollama:latest
    container_name: askvault-ollama-setup
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      - OLLAMA_HOST=http://ollama:11434
    entrypoint: >
      /bin/sh -c "
        ollama pull llama3.2:latest &&
        ollama pull nomic-embed-text &&
        echo 'Models ready.'
      "
    restart: "no"

  # ── AskVault — RAG application ────────────────────────────────────────────────
  askvault:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: askvault-app
    restart: unless-stopped
    depends_on:
      ollama:
        condition: service_healthy
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_URL=http://ollama:11434
      - LLM_MODEL=llama3.2:latest
      - EMBEDDING_MODEL=nomic-embed-text
      - PORT=8000
    volumes:
      - askvault_data:/app/data
      - askvault_cache:/app/reranker_cache
      - askvault_logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

volumes:
  ollama_models:
    driver: local
  askvault_data:
    driver: local
  askvault_cache:
    driver: local
  askvault_logs:
    driver: local
